# Root env is only for Prisma CLI commands such as pnpm db:migrate:deploy.
# Runtime apps load their own apps/<app>/.env.local file.
DATABASE_URL="postgresql://file_server:file_server@127.0.0.1:5432/file_server"
